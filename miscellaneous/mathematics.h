#ifndef MY_HEADER_FILE
#define MY_HEADER_FILE //not actually used outside of this file for duplication prevention
    // Code Here
    //if the header is not defined, then define it as code here
    #include <iostream>
    double dabs (double d)
    {
        return (d > 0) ? d : -d;
    }
    
#endif